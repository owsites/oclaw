---
summary: "CLI reference for `openwolf uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `openwolf uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
openwolf uninstall
openwolf uninstall --all --yes
openwolf uninstall --dry-run
```
